pipeline {
  agent any
     stages {
        stage ('clean') {
           steps {
             cleanWS ()
         }  
       }     
        stage ('build') {
          steps {
            sh 'mvn clean install -DskipTests'
          }  
        }
      } 
    } 
